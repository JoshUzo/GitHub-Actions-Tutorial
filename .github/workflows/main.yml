name: First Workflow

on: push

jobs: 
   build:
    runs-on: ubuntu-latest
    
    steps:
     - uses: actions/checkout@v3

     - name: Run a simple Github action
       run: |
            echo "Github actions is cool" 
            echo "God is good" 
    # - name: Simulating Error
    #   run: echo "error taking place"; exit 1
     
